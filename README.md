## Getting Started

ローカルでアプリケーションを起動するまでの手順を示す。

---

リポジトリをクローンします。
`git clone`

gem をインストールします。
`bundle install`

アプリケーションを起動します。
`bundle exec ruby myapp.rb`

http://localhost:4567 でアプケーションにアクセスできます。
