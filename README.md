## Getting Started

ローカルでアプリケーションを起動するまでの手順を示す。

---

リポジトリをクローンします。

`git clone`

gem をインストールします。

`bundle install`

アプリケーションを起動します。

`bundle exec ruby memo_app.rb`

http://localhost:4567/memos でアプケーションにアクセスできます。
